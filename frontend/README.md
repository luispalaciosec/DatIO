# frontend — renderer white-label (Ola 3)

Aquí vivirá la app React + Vite del reporte de cara al cliente (PT-09 renderer y tema,
PT-10 biblioteca de bloques, PT-11 export PDF). Consume únicamente `POST /consulta` del backend.

Se arranca cuando cierre PT-08 (resolvedores). La Fase 0 no incluye dashboard a propósito:
primero la captura diaria (ver README raíz).
